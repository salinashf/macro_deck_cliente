class SavedDevices {
  //modal class for Person object
  String id, idConnection, host, port;
  SavedDevices(
      {required this.id,
      required this.idConnection,
      required this.host,
      required this.port});
}
